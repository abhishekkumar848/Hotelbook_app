<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show Page</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }
        body {
            background-color: #f0f2f5;
            color: #333;
        }
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 20px;
            background-color: #fff;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        .navbar .logo {
            font-size: 24px;
            font-weight: bold;
            color: #0073e6;
        }
        .navbar .menu {
            display: flex;
            gap: 15px;
        }
        .navbar .menu a, .navbar button {
            text-decoration: none;
            color: #333;
            padding: 8px 12px;
            transition: background 0.3s ease;
        }
        .navbar button {
            background-color: #0073e6;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .navbar button:hover {
            background-color: #005bb5;
        }
        .container {
            max-width: 1000px;
            background: white;
            border-radius: 15px;
            padding: 20px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin: 50px auto;
        }
        .left-content {
            max-width: 50%;
        }
        .left-content h1 {
            font-size: 40px;
            margin-bottom: 10px;
        }
        .left-content p {
            font-size: 16px;
            margin-bottom: 20px;
        }
        .user-info {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .user-info img {
            width: 40px;
            height: 40px;
            border-radius: 50%;
        }
        .right-content {
            max-width: 45%;
            text-align: center;
        }
        .card {
            width: 100%;
            background: white;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            padding: 15px;
        }
        .card img {
            width: 100%;
            border-radius: 10px;
        }
        .price-tag {
            font-weight: bold;
            color: #e63946;
            margin-top: 10px;
        }
        .apply-btn {
            margin-top: 20px;
            padding: 10px 20px;
            background: black;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background 0.3s;
        }
        .apply-btn:hover {
            background: #e63946;
        }
        .swiper {
            width: 100%;
            height: 200px;
            margin-top: 20px;
        }
        .swiper-slide img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 10px;
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="logo">alloy</div>
        <div class="menu">
            <a href="#">Products</a>
            <a href="#">Integrations</a>
            <a href="#">Resources</a>
            <a href="#">Login</a>
            <button>Try it free</button>
        </div>
    </nav>
    <div class="container">
        <div class="left-content">
            <h1><%= listDetails.title %></h1>
            <p><%= listDetails.description %></p>
            <div class="user-info">
                <img src="<%= listDetails.image %>" alt="User Image">
                <span><%= listDetails.location %> / <%= listDetails.country %></span>
            </div>
            <p>⭐⭐⭐⭐⭐ 4.9</p>
            <button class="apply-btn">Apply Now — It’s Free</button>
        </div>
        <div class="right-content">
            <div class="card">
                <div class="swiper mySwiper">
                    <div class="swiper-wrapper">
                        <div class="swiper-slide"><img src="<%= listDetails.image %>" alt=""></div>
                        <div class="swiper-slide"><img src="<%= listDetails.image1 %>" alt=""></div>
                        <div class="swiper-slide"><img src="<%= listDetails.image2 %>" alt=""></div>
                    </div>
                </div>
                <br>
                <img src="<%= listDetails.image2 %>" alt="Credit Card Image"  style="height: 300px;" >
                <p class="price-tag">₹<%= listDetails.price %>8 Rupee</p>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
    <script>
        var swiper = new Swiper(".mySwiper", {
            spaceBetween: 30,
            centeredSlides: true,
            autoplay: {
                delay: 2500,
                disableOnInteraction: false,
            },
            pagination: {
                el: ".swiper-pagination",
                clickable: true,
            },
            
        });
    </script>
</body>
</html>
